PORT=8788

# Shared HMAC secret — must match AUTH_SECRET in the EXE environment
# Generate a strong random value: openssl rand -hex 32
AUTH_SECRET=cd612134ad2eb1d83c496e9200d427c5f58f3d755755db7f33e51bda8c7da898

# Allowed frontend origin for Socket.IO CORS (no trailing slash)
# Production example: https://bank-idx.com
ALLOWED_ORIGIN=http://localhost:5173

# Comma-separated whitelist of valid channel names (leave empty to allow any — dev mode only)
# Production example: demo,lobby,room1
ALLOWED_CHANNELS=d0ea6456acf9cedf
